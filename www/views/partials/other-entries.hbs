{{# if other_entries}}
	<form id="pickActionsForm" action="" method="POST">
		<select onchange="app.fetch_detail('detail-quiniela.html?id='+$(this).val()+'&entry={{entry.pool_id}}', {{entry.pool_id}}, 'live', $(this).val() ); setTimeout(function(){app.fill_entry_picks();},400);" id="select_other_entries" class="unique" name="select_other_entries">
			<option value="">Mis registros en esta quiniela</option>
			{{# entry}}
				<option value="{{id}}" selected>Postura {{number}}/{{ inc ../other_entries.length}}</option>
			{{/ entry}}
			{{# other_entries}}
				<option value="{{id}}">Postura {{number}}/{{ inc ../other_entries.length}}</option>
			{{/ other_entries}}
		</select>
	</form>
{{/ if}}